const validateFields = require('../../utils/validateFields');
const taskService = require('./task.service');


const create = async (req, res) => {
  const validatedFields = ['title', 'description', 'status', 'priority', 'dueDate', 'projectId'];


}
